#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int main(void)
{
	int primenumber; // 소수
	int number; // scanf 로 입력 할 정수

	for (int i = 0; i < number; i++)
	{
		for (int j = 0; j < number; j++)
		{
			scanf_s("%d", &number);

			if (number % number-1  == 0)
				printf("");
			else
				printf("%d\n", );
		}
		printf("\n");
	}
}
