#include <stdio.h>

// 함수 선언
double average(double a, double b);

int main(void)
{
    double res;
    res = average(1.5, 3.4);
    printf("Average: %lf\n", res);
    return 0;
}

// 함수 정의
double average(double a, double b)
{
    double temp;
    temp = a + b;
    return (temp / 2.0);
}
